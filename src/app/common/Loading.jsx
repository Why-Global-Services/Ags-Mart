import { FaSpinner } from "react-icons/fa";
import "./loader.css"; // Import the CSS file for styling

const Loading = () => {
    return (
        <div className="loader mx-auto flex flex-col items-center justify-center min-h-screen">
            Loading...  
        </div>
    )
}

export default Loading
